{
   "name": "packages",
   "version": "1.0.0",
   "description": "Sub-packages for the fullstack quiz-app",
   "main": "index.js",
   "scripts": {
      "build:types": "tsc -d types/index.ts",
      "watch:types": "tsc -w -d types/index.ts",
      "build:helpers": "tsc -d helpers/index.ts",
      "watch:helpers": "tsc -w -d helpers/index.ts",
      "build": "pnpm run build:types && pnpm run build:helpers",
      "watch": "pnpm run watch:types && pnpm run watch:helpers"
   },
   "keywords": [],
   "author": "Ayobami Ikewumi",
   "devDependencies": {
      "@vitest/coverage-c8": "^0.24.3",
      "@vitest/ui": "^0.24.3",
      "typescript": "^4.8.4",
      "vitest": "^0.24.3"
   },
   "dependencies": {
      "@types/markdown-it": "^12.2.3",
      "markdown-it": "^13.0.1",
      "mongoose": "*",
      "node-html-markdown": "^1.2.2",
      "vite": "^3.1.0"
   }
}